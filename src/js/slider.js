console.log("hello from slider");
const sectionSlider = document.querySelector(".section__slider");
const div = document.createElement("div");
div.textContent = "slider ";
sectionSlider && sectionSlider.append(div);
